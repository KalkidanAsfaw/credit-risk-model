version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_DIR=data/processed
    volumes:
      - ./data/processed:/app/data/processed:ro
    restart: unless-stopped
